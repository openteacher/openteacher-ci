- upload build artifacts to VPS
  - also setup irc bot/code documentation/web services again (should be easier
    with Docker image as base)
- openSUSE docker file (build-only)
- windows build (appveyor?)
